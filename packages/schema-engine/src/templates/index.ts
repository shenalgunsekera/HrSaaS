/**
 * Starter templates generated from HR_System_Feature_Sheets_v5.
 * Employee Master is the proof pattern; the remaining modules
 * (Attendance, Leave, Payroll, …) are transcribed in Phase 4.
 */
export { employeeMasterTemplate } from './employee-master';
